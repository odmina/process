These are classes to simulate data using Orstein-Uhlenbeck process and to add white noise.

To run some tests, use run_tests.py

Tests in the `tests` directory still require supervision (will be automated and added to run_tests.py soon).
